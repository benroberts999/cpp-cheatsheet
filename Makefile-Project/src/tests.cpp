#include <cassert>
#include <iostream>
// include anything else you might want

// Sometimes a single project will have two executables
// For example: one might be for unit tests
int main() {
  std::cout << "Tests:\n";

  // Implement all the tests

  bool all_tests_passed = true;
  assert(all_tests_passed);
  //
}